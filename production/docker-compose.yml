web:
  build: .
  command: /opt/app/bin/krihelinator foreground
  volumes:
    - .:/opt/app/  # read only
  ports:
    - 80:80
  links:
    - db
  restart: always

db:
  image: postgres:9.5
  restart: always
